interface:
  display_name: "月末简便结转销货（劳务）成本"
  short_description: "按收入明细、毛利率和可用存货余额简便结转月末销货与劳务成本"
  default_prompt: "Use $close-month-end-sales-cost to calculate and draft a simplified month-end sales and service cost carryforward."

dependencies:
  tools:
    - type: "mcp"
      value: "accounting"
      description: "Local ai-accounting-system MCP service"
      transport: "streamable_http"
      url: "http://127.0.0.1:8080/mcp"

policy:
  allow_implicit_invocation: true
