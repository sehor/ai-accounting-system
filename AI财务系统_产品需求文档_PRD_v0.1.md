# AI 财务系统产品需求文档（PRD）v0.1

> 文档状态：修订版  
> 更新日期：2026-07-27  
> 产品形态：后端财务平台（REST API、OpenAPI、MCP），本期不建设前端

## 1. 产品概述

本产品面向需要管理一个或多个账套的会计人员与财务团队，提供从账套初始化、凭证处理、附件归档到财务报表查询的完整后端能力。

产品不区分“企业内部会计”和“代账团队”两种业务模型。二者都通过账套管理业务数据，通过用户与账套的成员关系获得权限。未来如需开放给外部协作方，可接入专门的外部权限系统，不改变账套内的会计数据模型。

核心原则：

- 账套是会计数据与访问控制的边界。
- 用户与账套是多对多关系。
- AI 或规则模块负责提取、建议和校验，不替代授权用户作出正式记账决定。
- 业务数据允许修改、撤销和恢复，但任何变更必须留下不可覆盖的审计记录。
- 会计结果必须由确定性规则验证，不能依赖模型自由生成。

## 2. 产品目标

v0.1 需要实现：

- 支持一个用户管理多个账套、一个账套由多个用户协作。
- 每个账套独立设置会计准则、本位币、期间、科目和维度。
- 首期内置“小企业会计准则”，技术上保留新增其他中国会计准则的扩展能力。
- 支持多币种凭证，并以账套本位币生成账簿和报表。
- 支持凭证草稿、校验、可选审批、记账、反记账、历史版本恢复和冲销。
- 支持资产负债表、利润表、科目余额表、总账和明细账。
- 支持附件上传、异步处理和模拟的票据提取/凭证建议流程。
- 提供稳定的 REST API、OpenAPI 文档和受限 MCP 工具，供前端或 Coding Agent 后续接入。

## 3. 用户、账套与权限

### 3.1 关系模型

- `user` 与 `ledger` 为多对多关系。
- 关联实体 `ledger_membership` 保存用户在指定账套中的角色和状态。
- 所有账套业务表必须包含 `ledger_id`。
- 每次账套内请求都必须同时验证当前 `user_id` 与 `ledger_id` 的有效成员关系。

### 3.2 账套角色

| 角色 | 主要权限 |
|---|---|
| OWNER | 管理账套、成员、基础资料及全部会计操作 |
| EDITOR | 维护基础资料，创建、修改、校验和提交凭证 |
| REVIEWER | 审批或退回凭证，可查看全部账簿与审计记录 |
| VIEWER | 只读查询账簿、报表、附件和审计记录 |
| AGENT | 调用明确开放的查询、提取和草稿工具 |

权限约束：

- 记账、反记账、关账、重新开账和成员管理只能由获授权的人类用户执行。
- Agent 不能执行记账、反记账、关账、重新开账或成员管理。
- 默认不开启凭证审批；账套 OWNER 可启用审批流程。

## 4. v0.1 范围

### 4.1 包含

1. 外部 OIDC 身份登录与 JWT 鉴权。
2. 多账套与账套成员管理。
3. 小企业会计准则初始化包。
4. 会计科目、会计期间、辅助核算维度和本位币设置。
5. 期初余额手工录入与 CSV 导入。
6. 多币种凭证及手工汇率。
7. 凭证校验、可选审批、记账、反记账、恢复历史版本和冲销。
8. 基础账簿与法定报表。
9. PDF、JPEG、PNG 附件上传和 S3 兼容对象存储。
10. PostgreSQL 异步任务队列。
11. 可替换的票据提取接口；v0.1 仅提供 Mock 实现。
12. 重复单据检测与警告。
13. 审计日志、幂等控制、链路标识。
14. REST API、OpenAPI 和固定白名单 MCP 工具。

### 4.2 不包含

- Web、桌面或移动端界面。
- 真实 OCR、LLM 或第三方票据识别服务。
- RAG、向量数据库和自由问答。
- 自动获取汇率、期末汇兑损益和外币重估。
- 银行直连、支付、税务申报和电子发票平台直连。
- 通用工作流引擎、通用规则引擎或用户自定义脚本。
- 微服务拆分、Redis、RabbitMQ、Kafka。
- PostgreSQL RLS；v0.1 采用应用层账套隔离。
- 独立的外部协作权限系统。

## 5. 核心业务流程

### 5.1 创建账套

1. 用户创建账套，选择会计准则、本位币、启用日期及是否开启审批。
2. 系统复制指定版本的准则包，生成科目表和报表公式。
3. 创建人自动成为该账套 OWNER。
4. 用户可邀请其他已有用户，并为其分配账套角色。

### 5.2 录入期初余额

1. 用户手工录入或上传 CSV。
2. 系统校验科目、币种、方向、辅助核算项和借贷平衡。
3. 校验通过后确认期初余额。
4. 后续修改必须记录修订前后内容和操作者。

### 5.3 创建与记账凭证

1. 用户创建凭证草稿并录入分录。
2. 系统执行借贷平衡、期间、科目、币种、维度等确定性校验。
3. 未开启审批的账套：校验后可由授权用户直接记账。
4. 开启审批的账套：提交、审批通过后才能记账。
5. 记账在单个数据库事务中更新凭证状态及账簿影响。

### 5.4 上传单据并生成草稿

1. 用户上传附件，系统流式计算 SHA-256 并保存对象。
2. 重复检测命中时返回警告，不阻止上传或后续操作。
3. 异步任务调用 Mock 提取器生成结构化提取结果。
4. 用户可基于提取结果创建凭证草稿。
5. 原始模型输入输出不保存；仅保存结构化结果、来源引用、提供者/工具信息和哈希。

### 5.5 修改、撤销与恢复

- 草稿可直接修改，每次修改生成审计记录。
- 已记账凭证可由授权用户执行反记账，回到草稿状态后修改、重新校验并再次记账。
- 恢复任意历史版本时，不覆盖历史；系统以该版本内容创建新的当前修订，并进入草稿状态。
- 已记账凭证也可采用冲销方式处理。系统创建方向相反的冲销凭证，原凭证显示为“已冲销”，报表同时纳入原凭证和冲销凭证。
- 删除采用软删除并允许恢复；审计历史不能删除或重写。

### 5.6 查询财务数据

用户或 Agent 提交受限查询条件。系统只允许预定义指标、分组和过滤字段，生成参数化 SQL，并在当前账套范围内返回结果，不接受任意 SQL。

## 6. 功能需求

### 6.1 账套与成员

- 创建、查询、修改和停用账套。
- 设置会计准则、本位币、会计期间和审批开关。
- 添加、修改、停用账套成员关系。
- 账套停用后禁止新增会计业务，但允许查询和恢复。

### 6.2 会计准则与科目

- v0.1 内置版本化的小企业会计准则包。
- 准则包包含科目模板、科目类别、正常余额方向和报表公式。
- 创建账套时复制准则包快照，避免准则包升级自动改变已有账套。
- 账套可新增明细科目；已发生业务的科目不可物理删除，可停用。
- 后续准则通过新增版本化准则包实现，不在 v0.1 建设通用规则引擎。

### 6.3 会计期间

- 按月生成期间，状态为 `OPEN` 或 `CLOSED`。
- 已关闭期间不能新增、修改、记账或反记账。
- OWNER 可重新打开期间，操作必须记录原因和审计日志。

### 6.4 多币种

- 每个账套设置一个本位币。
- 凭证行可使用与本位币不同的原币。
- 原币金额保留 4 位小数，汇率保留 8 位小数，本位币金额保留 2 位小数。
- 换算采用 `HALF_UP`；系统保存原币、汇率和本位币金额，不使用二进制浮点数。
- 汇率由用户手工提供。账簿和 v0.1 报表统一以本位币展示。

### 6.5 凭证

凭证头至少包括：

- 账套、会计期间、凭证日期、凭证字号和编号。
- 摘要、状态、当前修订号、创建人、修改人。
- 是否由附件或 Agent 建议生成。

凭证行至少包括：

- 科目、借贷方向、原币、原币金额、汇率、本位币金额。
- 摘要和辅助核算维度。

确定性校验至少包括：

- 借方本位币合计等于贷方本位币合计。
- 金额大于零，方向只能是借或贷。
- 科目有效且未停用。
- 凭证日期属于开放期间。
- 必填辅助核算项完整。
- 凭证编号在账套、期间和凭证字内唯一。
- 请求版本与服务端当前版本一致。

### 6.6 审批

- 账套级配置，默认关闭。
- 开启后状态依次为 `DRAFT → VALIDATED → SUBMITTED → APPROVED → POSTED`。
- 审批人可退回，退回后进入 `DRAFT`。
- 退回修改、历史恢复和反记账后的修改均使用同一套修订与审计机制，不引入复杂的分支版本。

### 6.7 重复单据

- 使用文件哈希、票据号码、交易日期、金额和对方名称等条件识别疑似重复。
- 所有重复结果只返回警告，不硬阻断上传、建草稿、提交或记账。
- 用户继续操作的行为写入审计日志。

### 6.8 报表与账簿

v0.1 提供：

- 科目余额表。
- 总账。
- 明细账。
- 资产负债表。
- 利润表。

报表要求：

- 只计算已记账凭证和已确认的期初余额。
- 反记账后立即移除相应影响。
- 冲销凭证作为普通已记账凭证参与计算。
- 报表公式来源于账套创建时复制的准则包。
- 支持按期间、科目、币种和辅助核算维度过滤。

### 6.9 附件与异步任务

- 支持 PDF、JPEG、PNG，单文件最大 20 MB。
- 文件存入 S3 兼容对象存储，数据库仅保存元数据、对象键和哈希。
- 附件对象不可原位覆盖；替换附件创建新记录。
- 异步任务支持排队、运行、成功、失败、重试和待人工处理状态。
- 自动重试最多 3 次，建议间隔为 1、5、30 分钟。

### 6.10 审计

以下操作必须记录操作者、账套、时间、原因、请求链路、变更前后结构化内容和关联对象：

- 账套及成员变更。
- 基础资料和期初余额变更。
- 凭证创建、修改、提交、审批、记账、反记账、恢复、冲销、删除和恢复删除。
- 期间关闭与重新打开。
- 重复警告后继续操作。
- Agent 工具调用及其结构化结果。

业务数据允许修改和恢复，但审计记录只追加，不允许由业务接口修改或删除。

## 7. API 与 Agent 能力

### 7.1 REST API

- API 前缀为 `/v1`。
- 账套内资源路径必须包含 `/ledgers/{ledgerId}`。
- 写请求支持 `Idempotency-Key`。
- 可并发修改的资源使用版本号实现乐观锁。
- 金额和汇率在 JSON 中使用十进制字符串。
- 错误响应采用 Problem Details，并包含稳定错误码、字段错误、`traceId` 和可重试标志。

### 7.2 MCP 工具白名单

v0.1 仅开放固定工具：

- `finance_query`
- `list_ledgers`
- `get_voucher`
- `create_voucher_draft`
- `validate_voucher`
- `upload_document`
- `extract_document`
- `get_job_status`
- `create_voucher_draft_from_document`

所有工具必须绑定当前用户身份和明确的账套，不允许传入 SQL。MCP 不开放记账、反记账、审批、关账、重新开账或成员管理。

## 8. 状态与关键规则

### 8.1 凭证状态

未开启审批：

`DRAFT → VALIDATED → POSTED`

开启审批：

`DRAFT → VALIDATED → SUBMITTED → APPROVED → POSTED`

其他转换：

- `SUBMITTED → DRAFT`：退回修改。
- `POSTED → DRAFT`：反记账。
- 任意历史修订 → 新的 `DRAFT` 修订：恢复版本。
- `POSTED → 创建冲销草稿 → 冲销凭证 POSTED`：原凭证展示为 `REVERSED`。

### 8.2 不变量

- 所有业务读写都限制在当前 `ledger_id`。
- 权限由当前 `user_id + ledger_id` 的成员关系决定。
- 凭证借贷必须按本位币平衡。
- 记账、反记账和账簿影响必须在同一事务内完成。
- 同一幂等键和同一请求负载只能产生一次业务结果。
- 所有金额使用确定精度的十进制类型。
- 业务内容可恢复，审计历史不可覆盖。

## 9. 非功能需求

### 9.1 安全与隔离

- 使用外部 OIDC/OAuth 2.0 身份提供方，服务端验证 JWT。
- 应用层自动注入并校验账套范围，禁止客户端绕过。
- 需要覆盖跨账套读取、写入和对象引用的负向测试。
- 不重复引入 `tenant_id`，也不启用 PostgreSQL RLS。

### 9.2 性能目标

以单实例、常规中小企业账套为基准：

- 普通资源查询 P95 小于 500 ms。
- 单张凭证校验或记账 P95 小于 1 s。
- 常用月度报表 P95 小于 3 s。
- 大文件上传采用流式处理，不把完整文件载入内存。

### 9.3 可观测性

- 每个请求返回或透传 `traceId`。
- 记录 HTTP、数据库、异步任务、对象存储和业务命令指标。
- 日志不得输出 JWT、附件内容或敏感原始模型输入输出。
- 提供健康检查和就绪检查。

### 9.4 保留与删除

- 活跃账套的会计数据、附件和审计记录不自动过期。
- 账套停用或删除后满 10 年，可按明确的清理操作物理清除其业务数据和附件。
- 清理前必须支持导出，并记录清理批次、范围和操作者。
- 模型原始输入输出不落库；结构化结果及哈希按所属业务数据的期限保留。

## 10. v0.1 验收标准

1. 同一用户可加入至少两个账套，同一账套可加入至少两个用户。
2. 无成员关系的用户不能读取、修改或通过对象引用访问该账套数据。
3. 新账套可使用小企业会计准则科目表和报表公式完成初始化。
4. 系统可录入人民币本位币凭证和至少一种外币凭证，并正确换算、平衡和出表。
5. 可通过手工和 CSV 两种方式导入期初余额。
6. 未开启审批时可完成草稿、校验、记账；开启审批时未经批准不能记账。
7. 已记账凭证可反记账、修改、再次记账，完整保留每次修订和状态变化。
8. 可选择历史修订恢复为新的草稿，不覆盖原审计记录。
9. 可生成并记账冲销凭证，账簿与报表结果正确。
10. 重复附件和重复票据只产生警告，不阻断流程。
11. 科目余额表、总账、明细账、资产负债表和利润表与测试数据一致。
12. Mock 提取任务可排队、执行、重试、返回结构化结果并生成凭证草稿。
13. MCP 工具不能执行记账、反记账、审批、关账或成员管理。
14. 所有关键写操作支持审计、幂等和并发版本检查。
15. OpenAPI 文档、数据库迁移、自动化测试和本地运行说明可供开发者或 Coding Agent 独立执行。

## 11. 交付路线

### M0：工程基础与账套访问

- Spring Boot 工程、数据库迁移、OIDC/JWT、统一错误和可观测性。
- 账套、用户映射、成员角色及应用层隔离。

### M1：会计核心

- 小企业会计准则包、科目、期间、维度和期初余额。
- 多币种凭证、校验、可选审批、记账、反记账、修订恢复和冲销。
- 基础账簿与财务报表。

### M2：附件与任务

- S3 兼容附件存储、重复警告、PostgreSQL 任务队列。
- Mock 提取和从单据创建凭证草稿。

### M3：Agent 与交付加固

- 受限财务查询 DSL、固定 MCP 工具。
- 隔离、安全、性能、恢复和部署验证。

## 12. 产品边界说明

本版本提供会计业务能力和可追溯机制，但不宣称自动满足所有企业内部控制、行业监管或法定档案制度。正式上线前，部署方应根据其适用地区、行业和组织制度确定审批、留存、备份与权限策略。
